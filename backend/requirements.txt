Django>=5.0,<6.0
dj-database-url>=2.1
python-dotenv>=1.0
anthropic>=0.34
requests>=2.31
psycopg2-binary>=2.9   # only needed if you use Postgres via DATABASE_URL
djangorestframework>=3.15
django-cors-headers>=4.4
gunicorn>=22.0          # production WSGI server (Railway)
whitenoise>=6.7          # serves /admin/'s static files without a separate static host
